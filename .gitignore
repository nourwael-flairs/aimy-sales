.claude
design-system/
.impeccable

# The V3 build's own ledgers. Scoped to old/ when that build moved there, so a
# new root README.md can be tracked; unscoped, `readme.md` matched it too
# (core.ignorecase=true) and the new one could never be committed.
old/GAPS.md
old/README.md
old/AUDIT.md

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
